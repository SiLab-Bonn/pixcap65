version: "1.0"

profile:
  base:
    name: qodana.starter

  inspections:
    - inspection: PyPep8Inspection
      options:
        ignoredErrors:
          - E722
          - E352
    - inspection: PyBroadException
      enabled: False
    - inspection: PyBroadExceptionInspection
      enabled: False

    - inspection: PyUnresolvedReferencesInspection
      options:
        ignoredIdentifiers:
          - basil