[project]
name = "pixcap65_USER_NAME"
version = "0.0.3"
authors = [

]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "numpy>=2.3",
    "matplotlib",
    "tables",
    "pyyaml",
    "bitarray",
    "coloredlogs",
    "basil_daq",
    "scipy"
]

[project.urls]
Homepage = ""
Issues = ""

[project.optional-dependencies]
advanced = [
    "iminuit",
    "kafe2"
]

[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"
